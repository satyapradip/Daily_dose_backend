// claude.service.js — the AI service was implemented in google.service.js
// (uses Google Gemini under the hood, but named claudeService for compatibility)
module.exports = require("./google.service");
